# Python_assignment1
Python assignment for tutdude
1 ) "assignment1_t1.py"
in this file we have delcare variable a to take user input for 1st number, variable b to take user input for number 2.
Then we have variable c that store the addition of interger value of a & b similarly we have variable d which store the substraction of integer a &b, then we have e which holds the multiplication of integer of a &b then f which stores the division of integer values of a and b.
then tin print function we printed the Addition substraction multiplication and division of given numbers.
***********************
2) "assignment1_t2.py"

3) Here we have two variables a & b which take user input for first and last name.
4) in variable c we concatenate both a & b with space sepration.
5) in print fuction we print c with greetion message.

6) *******************************************************
